'use strict';

angular.module('commandsender.commands')
    .controller('Commands', ['$scope',
        function($scope) {

            var vm = {
                
            };

            $scope.vm = vm;
        }
    ]);